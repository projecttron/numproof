"""NumProof framework integrations — drop-in guardrails/validators.

Each submodule lazily imports its framework, so importing this package needs only the
`numproof` client. Install the framework you use (see each module's docstring).
"""
